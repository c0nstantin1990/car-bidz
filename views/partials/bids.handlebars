<div class="bids">
  {{#each this}}
    <section class="bid">
      <div class="meta">
        {{user.username}}
        on
        {{format_date created_at}}
      </div>
      <div class="text">
        {{bid_price}}
      </div>
    </section>
  {{/each}}
</div>